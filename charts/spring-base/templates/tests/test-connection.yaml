apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-base.fullname" . }}-test-connection"
  labels:
    {{- include "spring-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
