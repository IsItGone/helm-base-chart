apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flutter-base.fullname" . }}-test-connection"
  labels:
    {{- include "flutter-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flutter-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
